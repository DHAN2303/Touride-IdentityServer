@using System.Reflection
@using Touride.IdentityPlatform.Admin.UI.Configuration
@{
    var runtimeVersion = typeof(AdminConfiguration)
        .GetTypeInfo()
        .Assembly
        .GetCustomAttribute<AssemblyInformationalVersionAttribute>()?.InformationalVersion;
}

@runtimeVersion